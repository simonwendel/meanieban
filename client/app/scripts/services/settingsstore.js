;(function() {
    'use strict';

    angular
        .module('meanieBanApp')
        .factory('settingsStore', settingsStore);

    /** @ngInject */
    function settingsStore() {
        return {
        };
    }
})();
