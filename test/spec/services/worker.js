'use strict';

describe('Service: Worker', function () {

    beforeEach(module('meanieBanApp'));

    var Worker;
    beforeEach(inject(function (_Worker_) {
        Worker = _Worker_;
    }));

});
